requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
httpx>=0.27.0
python-dateutil>=2.9.0
pytz>=2024.1
tzdata>=2024.1   # zoneinfo data for Windows runtime
pydantic>=2.7.0
tenacity>=8.3.0
rich>=13.7.0

# Used by scripts/generate_og_cards.py to screenshot per-game share cards
playwright>=1.44.0

# Used by scripts/mirror_logos.py to downscale and re-encode school logos
# before they are committed — WIAA's originals are ~20KB each for a 200px
# mark, and the dashboard loads up to 89 at once.
Pillow>=10.3.0
